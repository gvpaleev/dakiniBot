import BaseScene, { SceneOptions } from 'telegraf/src/scenes/base';
import { Middleware, MiddlewareObj } from 'telegraf/src/middleware';
import WizardContextWizard, { WizardSessionData } from 'telegraf/src/scenes/wizard/context';
import Context from 'telegraf/src/context';
import SceneContextScene from 'telegraf/src/scenes/context';
export declare class WizardScene<C extends Context & {
    scene: SceneContextScene<C, WizardSessionData>;
    wizard: WizardContextWizard<C>;
}> extends BaseScene<C> implements MiddlewareObj<C> {
    steps: Array<Middleware<C>>;
    constructor(id: string, ...steps: Array<Middleware<C>>);
    constructor(id: string, options: SceneOptions<C>, ...steps: Array<Middleware<C>>);
    middleware(): import("telegraf/src/middleware").MiddlewareFn<C>;
    enterMiddleware(): import("telegraf/src/middleware").MiddlewareFn<C>;
}
//# sourceMappingURL=index.d.ts.map